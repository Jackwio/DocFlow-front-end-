import React from 'react';
import ReactDOM from 'react-dom/client';
import DesignsApp from './DesignsApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DesignsApp />
  </React.StrictMode>,
);
